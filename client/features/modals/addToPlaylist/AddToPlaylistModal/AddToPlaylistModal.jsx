import React from 'react';
import styled from 'styled-components';

function AddToPlaylistModal({ trackId }) {
  console.log(trackId);
  return <div>HI This is add to playlist modal</div>;
}

export default AddToPlaylistModal;
