Main entry route where users can access top charts sound tracks by selecting category.

Now that we have upgraded our router to v4, all <Router>'s are moved inline as a real react component. Checkout App.jsx for main routing.