import * as colors from './colors';

const theme = {
  bgColor: colors.lighterBlack,
  bgColorSub: colors.lightBlack,
  fontColor: colors.lighterGray,
  fontColorSub: colors.gray,
  themeColor: colors.orange,
  boxShadowColor: colors.transparentLight,
  separatorColorDark: colors.black,
  separatorColorClean: colors.darkGray,
};

export default theme;
