import * as colors from './colors';

const fontSize = {
  default: '1rem',
  xsm: '0.8rem',
  sm: '0.95rem',
  md: '1rem',
  lg: '1.25rem',
  xlg: '1.5rem',
};

const theme = {
  colors,
  fontSize,
};

export default theme;
