var expect = require('expect');


// use describe() to group your tests.
describe('App', () => {
  it('should properly run tests, this is the desc of our test', () => {
    expect(1).toBe(1);
  });
});
